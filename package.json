{
  "name": "DefaultAppName",
  "version": "0.1.0",
  "license": "MIT",
  "engines": {
    "node": "10.1.0",
    "yarn": "1.6.0"
  },
  "scripts": {
    "build": "yarn compile && yarn deploy && yarn routes && rm public/index.html",
    "compile": "yarn --cwd client install && yarn --cwd client build",
    "routes": "cp -a client/src/routes.json public/routes.json",
    "deploy": "cp -a client/build/. public/",
    "heroku-postbuild": "yarn build"
  }
}
