#!/usr/bin/env bash
git branch -D temp
git checkout -b temp

bin/dev_setup

mv credentials.sample.yml.enc credentials.yml.enc
git add -A
git commit -am "push to heroku"
git push heroku +temp:master

bin/reset

git stash
git checkout master
git branch -D temp
